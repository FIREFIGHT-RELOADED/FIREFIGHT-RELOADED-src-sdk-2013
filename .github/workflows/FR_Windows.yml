name: FR_Windows

on: 
  push:
    paths:
      - '.github/**'
      - 'sp/src/**'
  pull_request:
   paths:
      - 'sp/src/**'

jobs:
  build:
    runs-on:
      windows-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Configure dependencies
        uses: ilammy/msvc-dev-cmd@v1
        
      - name: Create project
        run: cd sp/src;devtools/bin/vpc.exe /fr +gameandtools /mksln firefight.sln

      - name: Build project
        run: cd sp/src;devenv firefight.sln /Build Release

      - name: Pack
        shell: bash
        run: tar cJfv Windows.tar.xz sp/game

      - name: Upload product
        uses: actions/upload-artifact@v2
        with:
          name: firefightreloaded_windows
          path: Windows.tar.xz
